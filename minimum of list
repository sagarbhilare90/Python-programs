#minimum element in list
num1=[1,2,3,4]
def minm(num):
    temp=num[0]
    for i in range(1,len(num)-1):
        if num[i]< temp:
            temp=num[i]
    return temp
print("minimum of array is : ", minm(num1))
